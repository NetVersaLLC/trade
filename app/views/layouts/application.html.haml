!!!
%html
  %head
    %title Tradebitcoin.com: Buy Bitcoins, Trade Bitcoins
    %link{:href => "http://fonts.googleapis.com/css?family=Neuton", :rel => "stylesheet", :type => "text/css"}/
    %script{:language => "JavaScript", :src => "http://j.maxmind.com/app/geoip.js"}
    %script{:src => "https://maps.googleapis.com/maps/api/js?v=3.exp&sensor=true"}
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    .menu
      - if user_signed_in?
        - if current_user.name.present?
          Signed in as: #{current_user.name} | #{link_to 'Sign Out', destroy_user_session_path, :method => :delete}
        - else
          Signed in as: #{current_user.email} | #{link_to 'Sign Out', destroy_user_session_path, :method => :delete}
        |
        = link_to "Edit account", edit_user_registration_path
      - else
        = link_to "Sign in", new_user_session_path
        |
        = link_to "Sign up", new_user_registration_path

    #logo
      %a{:href => "/under_construction"}
      -# %a{:href => "/authentications?mode=join"}
        = image_tag 'join.png', :alt => 'Join', :id => :join
      %a{:href => "/about"}
        = image_tag 'about.png', :alt => 'About', :id => :about
      %a{:href => "/under_construction"}
      -#%a{:href => "/authentications"}
        = image_tag 'members.png', :alt => 'Members Login', :id => 'members'
      #instructions Find local Bitcoin traders in your area.
  
    #content
      - unless flash.empty? 
        - flash.each do |name, msg|
          = raw(msg)
      = yield

      #bottom
        %p
          %a{:href => "/tos"} Terms of Service
          |
          %a{:href => "/privacy_policy"} Privacy Policy
          |
          %a{:href => "/transactions"} Transaction Guide
          |
          %a{:href => "/thanks"} Thanks
          %br/
        #copyright
          Copyright &copy; 2011
          %a{:href => "http://assuredwebdevelopment"} Assured Web Development
          and Jonathan Jeffus
          %br/
          Donations?:  12SW1w5mvp9ExwMbM9kMwTbN4D9e75KQDr
